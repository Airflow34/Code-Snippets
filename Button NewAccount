struct NewAccountButtonView: View {
    var body: some View {
        Button {
            // Login Button drücken
            
        } label: {
            HStack {
                Spacer()
                Text("Account erstellen")
                    .bold()
                    .foregroundColor(Color.white)
                Spacer()
            }.padding().background(Color.black).cornerRadius(5).shadow(radius: 10).padding()
        }
    }
}
